# Homelab registry is HTTPS at registry.maddscientist.com (Let's Encrypt).
# BuildKit no longer needs http=true / insecure for pushes to this host.
# Old NodePort HTTP entry removed; delete this file once CI stops referencing it.
