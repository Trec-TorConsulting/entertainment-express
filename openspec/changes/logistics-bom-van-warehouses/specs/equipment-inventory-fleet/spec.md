## ADDED Requirements

### Requirement: Real-Time Mobile Van Inventory Manifest
The system SHALL provide a live inventory manifest for each vehicle warehouse, indicating which serialized assets and quantities are currently loaded on that vehicle.

#### Scenario: Inspecting vehicle contents
- **WHEN** an operator or dispatcher views a fleet vehicle record
- **THEN** the system lists all active stock and serialized assets residing in that vehicle's warehouse location
